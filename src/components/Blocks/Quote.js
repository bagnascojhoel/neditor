import styled from 'styled-components'
import { fonts, colors } from '../../utils/styles/helpers';

const Quote = styled.p`
  position: relative;
  padding: 5px;
  font-family: ${fonts.serif};
  border-left: solid 3px ${colors.ligthBlue};
`

export default Quote
